# =============================================================================
# GenAssist Frontend Environment Configuration
# =============================================================================
# Copy this file to .env and update the values according to your setup.

# -----------------------------------------------------------------------------
# Application Version
# -----------------------------------------------------------------------------
# Version identifier displayed in the UI (e.g., footer, about section)
VITE_UI_VERSION=v1.0.0

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# Base URL for the backend REST API endpoints
# Examples:
#   - Local development: http://localhost:8000/api
#   - Production: https://api.yourdomain.com/api
VITE_PUBLIC_API_URL=http://localhost:8000/api

# WebSocket URL for real-time communication with the backend
# Must use ws:// for HTTP or wss:// for HTTPS connections
# Examples:
#   - Local development: ws://localhost:8000/api
#   - Production: wss://api.yourdomain.com/api
VITE_WEBSOCKET_PUBLIC_URL=ws://localhost:8000/api

# API key for authenticating requests to the GenAssist Chat service
# Obtain this from your GenAssist platform's API key management section
VITE_GENASSIST_CHAT_APIKEY=apikey_example

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
# Enable multi-tenant support for serving multiple organizations/workspaces
# Set to "true" to enable tenant isolation and workspace separation
VITE_MULTI_TENANT_ENABLED=true

# Enable WebSocket connections for real-time chat updates and streaming responses
# Set to "false" to fall back to HTTP polling
VITE_WS=true

# -----------------------------------------------------------------------------
# Onboarding Configuration
# -----------------------------------------------------------------------------
# These settings configure the onboarding experience shown to first-time users.

# Base URL for the onboarding chat API (typically the same as the main backend)
VITE_ONBOARDING_API_URL=http://localhost:8000/

# API key for the onboarding chat workflow
# Obtain this from the GenAssist Agent Workflow configured specifically for onboarding
VITE_ONBOARDING_CHAT_APIKEY=onboarding_apikey_example

# Credentials for the onboarding user account
# Used to automatically log in the user and create the Agent from the onboarding agent
VITE_ONBOARDING_USERNAME=username
VITE_ONBOARDING_PASSWORD=password
